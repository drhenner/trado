<% content_for :header_1, Store::settings.name %>
<% content_for :header_2, "Order ##{@order.id} confirmation" %>
<% content_for :additional_info, "By placing this order you will have agreed to our terms and conditions. If you are a consumer ordering from the European Union then directive 2011/83/EU will apply to your order (also known as The Directive on Consumer Rights). This includes the right to return an item within 14 days of receipt, provided the item is not customised or assembled to-order. If you wish to request a cancellation please use the cancel form here, ensuring that it is sent as soon as possible within the 14 day limit. Returns/cancellations will not be accepted if the items have been modified or damaged by the user. We require that customers cover the return postage costs of any non-faulty returned items." %>

Hello <%= @order.delivery_address.full_name %>,

Thanks for your order. We'll let you know once your item(s) have dispatched.

Your order will be sent to
---------------------
<%= @order.delivery_address.full_name %>
<%= @order.delivery_address.address %>
<%= @order.delivery_address.city %>
<%= @order.delivery_address.postcode %>
<%= @order.delivery_address.country %>
<%= @order.delivery_address.telephone %>

Your delivery option
---------------------
<%= @order.delivery.full_name %>

Your billing address
---------------------
<%= @order.billing_address.full_name %>
<%= @order.billing_address.address %>
<%= @order.billing_address.city %>
<%= @order.billing_address.postcode %>
<%= @order.billing_address.country %>
<%= @order.billing_address.telephone %>

Payment method
---------------------
<%= @order.payment_type.titleize %>


Contact email
---------------------
<%= @order.email %>

Order details
---------------------
Placed on <%= @order.created_at.strftime("#{@order.updated_at.day.ordinalize} %b %Y") %>

<% @order.order_items.each do |item| %>
    <%= item.sku.product.name %> | <%= Store::Price.new(price: item.price, tax_type: 'net').single %> | <%= item.quantity %> | <%= Store::Price.new(price: item.total_price, tax_type: 'net').single %>
<% end %>

Subtotal: <%= Store::Price.new(price: @order.net_amount, tax_type: 'net').single %>
Delivery: <%= Store::Price.new(price: @order.delivery.price, tax_type: 'net').single %>
<%= Store::settings.tax_name %>: <%= Store::Price.new(price: @order.tax_amount, tax_type: 'net').single %>
Total: <%= Store::Price.new(price: @order.gross_amount, tax_type: 'net').single %>

Questions about your order? Contact support@gimsonrobotics.co.uk and quote your order number.

Thanks for shopping with us!
<%= Store::settings.name %> team