#validateCartItemModal.modal.fade{"aria-hidden" => "false", "aria-labelledby" => "validateCartItemModalLabel", role: "dialog", tabindex: "-1", style: 'display:none;'}
  .modal-header
    %button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"} ×
    %h3#notifyMeModalLabel
      %span.light Insufficient
      Stock
  .modal-body
    %p 
      Unable to add the following item to your cart:
    %p
      %b= sku.product.name
      = "(#{sku_attribute_values(sku, sku.product.single)})" unless sku.product.single
    %p 
      There are only
      %b= sku.stock
      left in stock.
      